---
import Base from '../../layouts/Base.astro';
import law from '../../generated/law.json';
import OnPage from '../OnPage.astro';
import LangBanner from '../LangBanner.astro';
import DraftNotice from '../DraftNotice.astro';
import { dict } from '../../lib/i18n.mjs';
interface Props { lang: string }
const { lang } = Astro.props;
const t = dict(lang);
const doc = law.registration[lang] || law.registration.en;
const isLocal = Boolean(law.registration[lang]);
---
<Base title={t.registration.title} lang={lang} description={t.registration.description}>
  <p class="eyebrow">{t.registration.eyebrow}</p>
  <h1>{t.registration.title}</h1>
  <p class="intro prose">{t.registration.intro}</p>
  <LangBanner lang={lang} doc={law.registration[lang]} />
  <DraftNotice lang={lang} />
  <div class="withtoc">
    <OnPage lang={lang} toc={doc.toc} />
    <div class="prose" lang={isLocal ? lang : "en"} set:html={doc.html} />
  </div>
</Base>

<style>
  h1 { font-size: 1.7rem; margin: 0.4rem 0 1rem; }
  .intro { margin-bottom: 2rem; }
</style>
