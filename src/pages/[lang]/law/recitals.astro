---
import Recitals from '../../../components/screens/Recitals.astro';

export function getStaticPaths() {
  return ['nl', 'fr', 'de', 'es'].map((lang) => ({ params: { lang } }));
}
const { lang } = Astro.params;
---
<Recitals lang={lang} />
